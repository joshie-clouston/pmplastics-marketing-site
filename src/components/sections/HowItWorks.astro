---
const steps = [
  {
    number: '01',
    title: 'Tell Us What You Need',
    description: 'Bring your concept, drawing, sample, or just an idea. We consult on design, materials, and the best manufacturing approach for your project.',
  },
  {
    number: '02',
    title: 'We Fabricate It',
    description: 'Precision manufacturing using our full range of CNC equipment. From one-off prototypes to production runs — we handle it all in-house.',
  },
  {
    number: '03',
    title: 'Pick Up or Delivery',
    description: 'Collect from our Burleigh Heads workshop or we deliver to you. Fast turnaround on every project, no matter the size.',
  },
];
---
<section class="section bg-muted" id="how-it-works">
  <div class="container mx-auto">
    <div class="text-center mb-12" data-animate="fade-up">
      <h2 class="text-3xl sm:text-4xl font-800 tracking-tight mb-4">How It Works</h2>
      <p class="text-lg text-muted-foreground max-w-2xl mx-auto">
        From concept to creation in three simple steps.
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-8 relative" data-animate="stagger">
      <!-- Connector line (desktop) -->
      <div class="hidden md:block absolute top-8 left-[16%] right-[16%] h-0.5 bg-border"></div>

      {steps.map((step) => (
        <div class="relative text-center">
          <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-primary text-primary-foreground text-xl font-800 mb-6 relative z-10">
            {step.number}
          </div>
          <h3 class="font-700 text-xl mb-3">{step.title}</h3>
          <p class="text-muted-foreground leading-relaxed max-w-xs mx-auto">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
