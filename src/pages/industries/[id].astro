---
import { getCollection, render } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

export const prerender = true;

export async function getStaticPaths() {
  const industries = await getCollection('industries', ({ data }) => !data.draft);
  return industries.map((entry) => ({
    params: { id: entry.id },
    props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await render(entry);
---
<BaseLayout title={`${entry.data.title} — Plastic Fabrication`} description={entry.data.description}>
  <section class="section bg-secondary text-secondary-foreground">
    <div class="container mx-auto max-w-3xl">
      <a href="/industries" class="inline-flex items-center gap-1 text-sm font-medium text-gray-400 hover:text-white mb-6 transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
        All Industries
      </a>
      <h1 class="text-4xl sm:text-5xl font-900 tracking-tight mb-4" data-animate="fade-up">
        {entry.data.headline || entry.data.title}
      </h1>
      <p class="text-lg text-gray-400" data-animate="fade-up">{entry.data.description}</p>
    </div>
  </section>

  <section class="section bg-white">
    <div class="container mx-auto max-w-3xl">
      <div class="prose prose-lg max-w-none" data-animate="fade-up">
        <Content />
      </div>

      {entry.data.services.length > 0 && (
        <div class="mt-12 p-6 bg-muted rounded-xl" data-animate="fade-up">
          <h2 class="font-700 text-xl mb-4">Services for {entry.data.title}</h2>
          <ul class="grid sm:grid-cols-2 gap-2">
            {entry.data.services.map((service) => (
              <li class="flex items-start gap-2 text-sm">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-primary flex-shrink-0 mt-0.5"><polyline points="20 6 9 17 4 12"/></svg>
                {service}
              </li>
            ))}
          </ul>
        </div>
      )}

      {entry.data.products.length > 0 && (
        <div class="mt-6 p-6 bg-muted rounded-xl" data-animate="fade-up">
          <h2 class="font-700 text-xl mb-4">Common Products</h2>
          <ul class="grid sm:grid-cols-2 gap-2">
            {entry.data.products.map((product) => (
              <li class="flex items-start gap-2 text-sm">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" class="text-accent flex-shrink-0 mt-0.5"><polyline points="20 6 9 17 4 12"/></svg>
                {product}
              </li>
            ))}
          </ul>
        </div>
      )}
    </div>
  </section>

  <section class="section bg-muted">
    <div class="container mx-auto text-center">
      <h2 class="text-2xl sm:text-3xl font-800 mb-4">Need plastic fabrication for {entry.data.title.toLowerCase()}?</h2>
      <p class="text-muted-foreground mb-6">Tell us about your project.</p>
      <div class="flex flex-col sm:flex-row gap-3 justify-center">
        <a href="/quote" class="btn btn-primary btn-lg">Get a Quote</a>
        <a href="tel:0755357544" class="btn btn-outline btn-lg">Call 07 5535 7544</a>
      </div>
    </div>
  </section>
</BaseLayout>
